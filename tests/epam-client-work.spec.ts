import { test, expect } from '@playwright/test';

test.describe('EPAM website - Client Work navigation', () => {
  test('navigate via Services to Client Work', async ({ page }) => {
    // Test implementation will be added in the next commit
  });
});
